# BatchConv Android Build

## Quick build
```bash
pip install buildozer cython
buildozer android debug
```

The Android launcher uses a Kivy splash screen, starts the local Flask server, and opens the app UI in a WebView.
